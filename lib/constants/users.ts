/**
 * User-related constants.
 */
const USER_CONSTANTS = {
    /**
     * Redirection to the sign-in route via the auth middleware.
     */
    SIGN_IN_REDIRECT: "/sign-in",
}

export default USER_CONSTANTS
